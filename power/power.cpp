#include <stdio.h>
#include "power.h"
using namespace std;

int powerLevel = 32;

bool powerCheck() {
    // If battery level < 20, return false. Otherwise, return true
    return false;
}
void resetPower(){
	powerLevel =32; //Back to inital state 
}
